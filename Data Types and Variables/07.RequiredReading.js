function reading(numberOfPages, pagesRead, numberOfDays){

    let totalTime= numberOfPages/pagesRead
    let neededHours=totalTime/numberOfDays

    console.log(neededHours);
}

reading(212,20,2)
reading(432,15,4)